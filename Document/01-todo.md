# TODO

미해결 작업 목록. 최종 갱신 2026-08-27.

사용법과 원리는 `02-guide.html`, 도면에서 값 뽑는 법은 `03-drawing-requirements.md`.
예전 스크립트 방식의 근거는 `00-overview.md`.

---

## 0. 지금 상태

플러그인으로 전환 완료. LED 벽과 프로젝터 양쪽을 다룬다.

```
493c8b9  프로젝터 배치 검토와 프로젝터별 영상 변환 추가
14b6654  각진 코너 프리셋 추가 + 배치 휴리스틱과 이음매 산출 버그 수정
```

프리셋 2개가 있다. **둘 다 실제 현장 값이 아니라 방법 검증용 예시다.**

| 프리셋 | 벽 | 가운데 대 입사각 | 밀도차 |
|---|---|---|---|
| `indoor_1_10` | 필렛 R=0.25 m | 33.7 ~ 90° | 2.7배 |
| `indoor_corner90` | 각진 90° | 36.0 ~ 45.9° | 1.7배 |

---

## 1. 바로 할 것

- **`.claude/` 커밋 여부 결정.** 자동 커밋 훅(`settings.json` + `auto-commit.sh`)이다.
  팀과 공유할 워크플로면 커밋하고, 개인 설정이면 `.gitignore` 에 넣는다
- **`/hooks` 를 한 번 열거나 재시작.** `.claude/` 가 세션 시작 시점에 없었어서 설정
  감시자가 그 디렉터리를 안 보고 있다. 그때까지 자동 커밋이 발동하지 않는다
- **`Document/.img.b64` 삭제.** HTML 에 이미지 넣으려고 만든 임시 파일 (224 KB)

---

## 2. 미완 기능

### 2.1 워프 메시 / PFM 출력 (7단계)

프로젝터 파이프라인 1~6 단계는 끝났다. 남은 것은 nDisplay 실시간용 출력이다.

```
1~3  프로젝터 정의 + 밀도/입사각 맵          완료
4~5  이음매 자동 산출 + 알파 블렌드 맵        완료
6    프로젝터별 영상 변환 (2경로)            완료
7    워프 메시 / PFM 출력                    미완
```

**먼저 정할 것: 현장이 언리얼 실시간인가, 플레이어 재생인가.**
실시간이면 7번이 목적지이고 6번은 필요 없다. 플레이어면 그 반대다.

### 2.2 벽 이미지 해상도 병목

프로젝터 N대의 총 화소가 벽 이미지 한 장을 넘으면 2단계 파이프라인이 병목이다.
N >= 2 면 항상 그렇다. **축척과 무관하다** (밀도가 오르는 만큼 길이가 줄어 곱이 같다).

지금은 합성 파이프라인으로 우회했다 (중간본을 아예 안 만든다). 벽 이미지가 실체로
필요한 경우 — 콘텐츠 검수, 외부 납품 — 는 이미지 시퀀스로 가야 한다. 아직 안 만들었다.

### 2.3 `res_w = 2560` 이 아직 고정

`menu._opts()` 에 하드코딩돼 있다. nDisplay 창 크기와 MRQ 출력 해상도를 정한다.
실제 LED 벽 해상도와 멀티 노드 클러스터 구성은 아직 반영 안 됨.

### 2.4 다중 노드 (LED 프로세서 입력 분할)

`build.apply_regions()` 가 노드 1개를 전제한다 (`len(nodes) == 1` assert).
LED 프로세서가 패널별로 입력을 따로 받으면 노드를 쪼개야 한다. 프로세서 스펙 대기.

### 2.5 `column_us` 가 R=0 에서 퇴화 열을 만든다

`BentWall(fillet_r=0)` 은 이제 죽지 않는다 (0 나눗셈 가드 완료). 하지만 `column_us` 가
같은 값을 호 분할 수만큼 되풀이해 **중복 열 24개**를 만들고, 워프 메시에 퇴화 삼각형이 생긴다.

우회로 둘 다 있다: `fillet_r = 1.0` (호 1.6 cm, 사실상 직각) 또는 **다중 패널로 모델링**
(`indoor_corner90` 이 그 방식). 진짜 직각이면 다중 패널이 맞다.

---

## 3. 현장 값 확인 대기

**지금 계산은 전부 예시 값 위에 서 있다.** 실제 도면이 오면 다시 돌려야 한다.
필요한 항목은 `03-drawing-requirements.md` 에 정리돼 있다.

- **전개 길이, 높이, 꺾임각, 라운드 R** — 지금 값은 협의 기준값이지 도면 확정값이 아니다
- **볼록/오목** — 오목이면 수평 화각이 48° → **146°** 로 폭증한다 (확인함).
  단일 뷰포트로는 감당이 안 되고 프로젝터 배치도 통째로 달라진다
- **관람 거리와 눈높이** — `눈높이 1.6 m` 는 일반값
- **두 지대의 표고 차** — 벽이 선 지대와 관람 지대가 다르면 반드시 필요
- **프로젝터 사양** — 해상도, 렌즈 화각 범위, 광량(lm). 대수의 하한을 밝기가 정한다
- **캘리브레이션 방식** — 카메라 측정(VIOSO 등)인가 CAD 계산인가.
  측정 기반이면 곡면이 부담이 아니고, 7단계의 성격도 달라진다

---

## 4. 영상 색 패스스루 (우선순위 낮음)

프리뷰 정확도 문제다. `_curved.mp4` 를 LED 프로세서에 직접 넣으면 엔진을 안 거치므로
영상 경로에서는 이 문제가 없다.

`영상 1:1 패스스루` 를 켜면 자동노출과 ACES 톤커브가 꺼진다. 측정 결과
**최대 채널 오차 136 → 71.** 0 이어야 할 채널의 누출(탈색)은 거의 잡혔다.

남은 문제: 주 채널이 어두워지는데 **채널마다 비율이 다르다** (0.50 ~ 0.94).
노출 문제라면 세 채널이 같은 비율로 줄어야 한다. 3x3 색공간 행렬이 아직 끼어 있다는 뜻.

확인할 것:
- Working Color Space 프로젝트 설정
- `M_MediaPlate_Opaque` 가 Lit 인지 Unlit 인지 (Lit 이면 태양광/스카이라이트 색이 곱해진다)
- `r.TonemapperOutputDevice` 등 출력 디바이스 변환
- 포스트프로세스 Color Grading 기본값이 실제로 중립인지

판단: **여기서 멈추고 LED 프로세서에서 캘리브레이션** 하는 것이 현장에서 흔한 방식이다.

재측정: `영상 파일` 을 `TestPattern_wall.mp4` 로 두고 빌드 → 실행 → 화면 캡처 →
원본 프레임과 같은 좌표의 컬러바 픽셀 비교. 컬러바 x = 210/640/1065/1490/1920/2350, y = 120.

> 주의: `MediaExample` 계열 영상은 **평균 휘도 0.0 인 거의 완전 검정**이다. 원본부터 그렇다.
> 눈으로 확인할 때는 반드시 `TestPattern` 쪽을 쓸 것.

---

## 5. 정리 대상 (파일 삭제는 안 했음)

- `_deprecated/` 폴더 전체 (구 round / round_video / square 일체)
- `Content/VprodProject/Maps/{Round, RoundVideo, Square}.umap`
- `Content/Round/` (구 `NDC_Round_1Screen`)
- `Content/Movies/*_wall_round.mp4`, `*_mask.png` (삭제된 외곽 마스크와 짝이던 파일)
- `NDC_AnamorphicRig.uasset`, `SM_AnamorphicWall.uasset`, `testWell.umap`
  (프리셋별 에셋 이름을 쓰기 전의 잔재)
- `Config/AnamorphicRig/probe_site*.json` (테스트 프리셋 4개)
- `Document/02-guide.md` (HTML 만 쓰기로 함)
- 루트 `.gitignore` 맨 위의 웹 프로젝트 잔재 (`node_modules/`, `.next/`, `.vercel/`, `.pnp`)
- `CLAUDE.md` 에 다른 프로젝트(web2d) 템플릿이 그대로 남아 있다

---

## 6. 플러그인이 해결한 옛 항목 (기록)

| 옛 항목 | 지금 |
|---|---|
| `.bat` 재빌드가 에디터에 잠겨 조용히 실패 | 메뉴에서 직접 실행 |
| MRQ 가 `.ndisplay` 를 안 읽어 해상도가 안 바뀜 | `apply_regions()` 가 DCRA 에 반영 |
| 형상 knob 을 `geom_key()` 에 빠뜨리면 낡은 맵 재사용 | `geom_key()` 로 일원화 |
| `anamorphic.py` 가 대칭 2모니터만 | `PanelChain` N패널, 크기·해상도 제각각 |
| 좌우 면 길이가 같아야 함 | `face_b` / `face_a` 분리 |
| 스위트스팟이 대칭축 위에만 | `eye_offset_m` |
| `FILLET_R = 0` 에서 0 나눗셈으로 죽음 | 가드 완료 (퇴화 열은 2.5 참고) |
| 설정이 스크립트 상수 | JSON 프리셋, 모달리스 탭에서 편집 |
